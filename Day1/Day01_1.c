#include<stdio.h>
#include<math.h>
int main()
{
    int n,i,sum=0;
    printf("enter the the number:");
    scanf("%d",&n);
    for(i-0;i<=n;i++)
    {
        sum=sum+i;
    }
    printf("sum of first %d natural numbers are %d",n,sum);
    return 0;
}
